# ADK runtime (google-agents-cli-adk import path is provided by ./google_agents_cli_adk)
google-adk>=1.0.0

# Agents CLI deployment toolchain (optional locally: uvx google-agents-cli setup)
# google-agents-cli>=0.5.0

# FastAPI ingestion layer
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# PostgreSQL connectivity
psycopg2-binary>=2.9.9
asyncpg>=0.30.0

# HTTP client for MCP JSON-RPC adapters
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.1
email-validator>=2.2.0
